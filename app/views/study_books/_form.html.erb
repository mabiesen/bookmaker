<%= form_with(model: study_book) do |form| %>
  <% if study_book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(study_book.errors.count, "error") %> prohibited this study_book from being saved:</h2>

      <ul>
        <% study_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-4">
    <%= form.label :study_author_ids, "Authors", style: "display: block;", class: "form-label" %>
    <%= form.collection_select :study_author_ids, StudyAuthor.order(:last_name), :id, :fullname, multiple: true, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= form.label :title, style: "display: block", class: "form-label" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= form.label :summary, style: "display: block", class: "form-label" %>
    <%= form.text_area :summary, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= form.label :number_of_pages, style: "display: block", class: "form-label" %>
    <%= form.number_field :number_of_pages %>
  </div>

  <div class="mt-4">
    <%= form.label :completed, style: "display: block", class: "form-label" %>
    <%= form.check_box :completed %>
  </div>

  <div class="mt-4">
    <%= form.submit %>
  </div>
<% end %>
